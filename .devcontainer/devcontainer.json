{
    "name": "EduPredict Environment",
    "image": "continuumio/anaconda3",
    "postCreateCommand": "conda create -n edupredenv python=3.10 && conda activate myenv && pip install -r requirements.txt",
    "customizations": {
      "vscode": {
        "extensions": [
          "ms-python.python",
          "ms-toolsai.jupyter"
        ]
      }
    },
    "forwardPorts": [8888], // jupyter server port
    "remoteUser": "codespace"
  }